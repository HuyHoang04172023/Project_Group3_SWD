@model A_LIÊM_SHOP.Models.ShippingOrderViewModel

@{
    ViewBag.Title = "Checkout";
}

<h1>Checkout</h1>

<form method="post" action="@Url.Action("CreateShippingOrder", "Cart")">
    <div>
        <label>From Name:</label>
        <span>@ViewBag.from_name</span>
        <input type="hidden" name="from_name" value="@ViewBag.from_name" />
    </div>
    <div>
        <label>From Phone:</label>
        <span>@ViewBag.from_phone</span>
        <input type="hidden" name="from_phone" value="@ViewBag.from_phone" />
    </div>
    <div>
        <label>From Address:</label>
        <span>@ViewBag.from_address</span>
        <input type="hidden" name="from_address" value="@ViewBag.from_address" />
    </div>
    <div>
        <label>To Name:</label>
        <span>@ViewBag.to_name</span>
        <input type="hidden" name="to_name" value="@ViewBag.to_name" />
    </div>
    <div>
        <label>To Phone:</label>
        <span>@ViewBag.to_phone</span>
        <input type="hidden" name="to_phone" value="@ViewBag.to_phone" />
    </div>
    <div>
        <label>To Address:</label>
        <span>@ViewBag.to_address</span>
        <input type="hidden" name="to_address" value="@ViewBag.to_address" />
    </div>
    <div>
        <label>To Ward Code:</label>
        <span>@ViewBag.to_ward_code</span>
        <input type="hidden" name="to_ward_code" value="@ViewBag.to_ward_code" />
    </div>
    <div>
        <label>To District ID:</label>
        <span>@ViewBag.to_district_id</span>
        <input type="hidden" name="to_district_id" value="@ViewBag.to_district_id" />
    </div>
    <div>
        <label>COD Amount:</label>
        <span>@ViewBag.cod_amount</span>
        <input type="hidden" name="cod_amount" value="@ViewBag.cod_amount" />
    </div>
    <div>
        <label>Service ID:</label>
        <span>@ViewBag.service_id</span>
        <input type="hidden" name="service_id" value="@ViewBag.service_id" />
    </div>
    <div>
        <label>Payment Method:</label>
        <span>@ViewBag.payment_method</span>
        <input type="hidden" name="payment_method" value="@ViewBag.payment_method" />
    </div>

    <div>
        <label>Items:</label>
        <table border="1">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in ViewBag.items)
                {
                    <tr>
                        <td>@item.name</td>
                        <td>@item.quantity</td>
                        <td>@item.price</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <br />
    <button type="submit">Create Shipping Order</button>
</form>
